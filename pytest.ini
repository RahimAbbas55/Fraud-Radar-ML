[pytest]
# Ensures `src` is importable as a package when running `pytest` from
# the project root, avoiding the "python -m src.train" vs "python
# src/train.py" import-path confusion encountered on Credit Risk ML Pipeline.
pythonpath = .
testpaths = tests